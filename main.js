$(document).ready(function(){
  page.init();
  // var inputData = _.template(templates.realTodos)
  // var redData = inputData({done: false, items: "Hello box"})
  // $('.todo-items').append(redData);
});


var page = {
  init: function(){

  },
  initEvents: function(){
    $
  },
  initStyling: function(){

  },




  loadTemplate: function(){
    //1. return the template that we want to load
    //2. compile the template from step 1,
    //    into a variable that is the _.template function
    //3. grab data from template.js,
    //      use _.map(data, function(elm) {
    //
    //4.  take compiledTemplate and feed it data,
    //5.  append the Stringrified HTML from step 4,
          //on to the page

    //})
  },
  getTemplate: function(todosTemplate) {
    return templates[todosTemplate]
  }

}


_.map(arrayOfData, function(elm,idx,arr) {

})

arrayOfData.map(function(elm,idx,arr){

})
